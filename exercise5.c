#include <stdio.h>

/* This is a comment. */
int main()
{
    int distance = 100;

    // this is also a comment
    printf("You are %d miles away.\n", distance);
	getchar();
	printf("I understand and get the syntax for most of the operations here\n"); 
    return 0;
}